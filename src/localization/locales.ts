/**
 * Re-export locales from data folder
 */
export {
  LOCALES,
  locales,
  type Locale,
  getSortedLocales,
  getLocaleKey,
  DEFAULT_LOCALE_KEY,
  defaultLocale,
  getCurrentLocale,
} from '../data/locales';
